// Sting, Number, Undefined, Null, Boolean, Symbol
const nome = "Arthur"; // Sting
const nome1 = "Arthur"; // Sting
const nome2 = "Arthur"; // Sting
const num1 = 10; // Number
const num2 = 10.55; // Number
let nomeAluno; // Undefined -> Não aponta para local nenhum na memória
const sobrenomeAluno = null; // Nulo -> Não aponta pra local nenhum na memória
const aprovado = false; // Boolean = true, false (lógico)

//console.log(typeof aprovado, aprovado)

let a = 3;
let b = a;
a = 2
console.log(a, b)
